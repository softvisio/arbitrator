title: Test
permissions: ["*"]

methods:
  test:
    title: Test
    params:
      - name: args
        schema:
          type: string

  upload:
    title: Test upload
    meta:
      type: upload
      uploadMaxSize: .inf # infinity
    params:
      - name: file
        schema:
          instanceof: File
      - name: data
        schema:
          type: [string, object]
